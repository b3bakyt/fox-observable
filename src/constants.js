const COMMAND = {
  avoid_next_operators: Symbol('avoid_next_operators'),
};

const FUNCTION_TYPE = {
  map: 'map',
  filter: 'filter',
  distinctUntilChanged: 'distinctUntilChanged',
};

module.exports = {
  COMMAND,
  FUNCTION_TYPE,
};
